## What

(One-sentence summary of what this PR changes.)

## Why

(Link to issue if applicable: `Closes #N`. Otherwise a one-paragraph rationale.)

## Test plan

- [ ] `tools/scripts/preflight.sh` passes locally
- [ ] New tests added for changed behaviour (or rationale below for "no new tests")
- [ ] If renderer change: screenshot before/after, or `WEKDE_TIME_DIAG=1` numbers

## Notes for reviewer

(Things you want me to look at, gotchas you couldn't resolve, etc.)
